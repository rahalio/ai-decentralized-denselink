/**
 * ExecuteListVenueProgrammes - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: venues
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListVenueProgrammes Use Case
 *
 * DDD: Application use case for list Venue.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListVenueProgrammesInput, ListVenueProgrammesOutput } from "../dto/venue.dto";
import type { ExecutionContextService, IdGeneratorService } from "@denselink/services/_shared/index.js";
import type { VenueRepository } from "../ports";

export class ExecuteListVenueProgrammes {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly venue: VenueRepository
  ) {}

  async execute(input: ListVenueProgrammesInput): Promise<ListVenueProgrammesOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.venId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.venue.listVenueProgrammes({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListVenueProgrammesOutput;
  }
}
