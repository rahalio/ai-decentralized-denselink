/**
 * ExecuteGetMeshPort - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: meshports
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetMeshPort Use Case
 *
 * DDD: Application use case for get Meshport.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetMeshPortInput, GetMeshPortOutput } from "../dto/meshport.dto";
import type { ExecutionContextService, IdGeneratorService } from "@denselink/services/_shared/index.js";
import type { MeshportRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetMeshPort {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly meshport: MeshportRepository
  ) {}

  async execute(input: GetMeshPortInput): Promise<GetMeshPortOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.mptId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).meshPortId) {
      throw new ValidationError("meshPortId is required");
    }
    const meshportId = (input as any).meshPortId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.meshport.getMeshPort({ meshPortId: meshportId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${meshportId}`);
    }

    return entity as GetMeshPortOutput;
  }
}
