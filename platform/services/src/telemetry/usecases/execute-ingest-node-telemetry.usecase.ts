/**
 * ExecuteIngestNodeTelemetry - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: telemetry
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteIngestNodeTelemetry Use Case
 *
 * DDD: Application use case for get Aggregate.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { IngestNodeTelemetryInput, IngestNodeTelemetryOutput } from "../dto/aggregate.dto";
import type { ExecutionContextService, IdGeneratorService } from "@denselink/services/_shared/index.js";
import type { AggregateRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteIngestNodeTelemetry {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly aggregate: AggregateRepository
  ) {}

  async execute(input: IngestNodeTelemetryInput): Promise<IngestNodeTelemetryOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.tlaId();

    // All field names extracted from OpenAPI spec: activeNodes, appId, capturedAt, geoBin, licenseId, sessionDurationMinutesAvg
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).appId) {
      throw new ValidationError("appId is required");
    }
    const appId = (input as any).appId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.aggregate.ingestNodeTelemetry({ appId: appId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${appId}`);
    }

    return entity as IngestNodeTelemetryOutput;
  }
}
