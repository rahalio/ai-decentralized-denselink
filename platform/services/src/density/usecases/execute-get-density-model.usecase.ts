/**
 * ExecuteGetDensityModel - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: density
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetDensityModel Use Case
 *
 * DDD: Application use case for get DensityModel.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetDensityModelInput, GetDensityModelOutput } from "../dto/density-model.dto";
import type { ExecutionContextService, IdGeneratorService } from "@denselink/services/_shared/index.js";
import type { DensityModelRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetDensityModel {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly densityModel: DensityModelRepository
  ) {}

  async execute(input: GetDensityModelInput): Promise<GetDensityModelOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.denId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).densityModelId) {
      throw new ValidationError("densityModelId is required");
    }
    const densitymodelId = (input as any).densityModelId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.densityModel.getDensityModel({ densityModelId: densitymodelId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${densitymodelId}`);
    }

    return entity as GetDensityModelOutput;
  }
}
