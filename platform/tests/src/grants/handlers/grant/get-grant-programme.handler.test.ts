/**
 * GetGrantProgramme handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: grants
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getGrantProgramme } from "@denselink/api-server/domains/grants/handlers";
import type { GrantsDDDDependencies } from "@denselink/api-server/domains/grants";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@denselink/tests/shared";
import { GRANTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetGrantProgramme", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: GrantsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<GrantsDDDDependencies>(
      GRANTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetGrantProgramme, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { grantProgrammeId: entity.id };

    await getGrantProgramme(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetGrantProgramme.execute).toHaveBeenCalledWith(expect.objectContaining({ grantProgrammeId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { grantProgrammeId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetGrantProgramme, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getGrantProgramme(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
