/**
 * GetMeshPort handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: meshports
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getMeshPort } from "@denselink/api-server/domains/meshports/handlers";
import type { MeshportsDDDDependencies } from "@denselink/api-server/domains/meshports";
import { createMeshport as createMeshportEntity } from "@denselink/factories/meshports";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@denselink/tests/shared";
import { MESHPORTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetMeshPort", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: MeshportsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<MeshportsDDDDependencies>(
      MESHPORTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createMeshportEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetMeshPort, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { meshPortId: entity.id };

    await getMeshPort(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetMeshPort.execute).toHaveBeenCalledWith(expect.objectContaining({ meshPortId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { meshPortId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetMeshPort, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getMeshPort(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
