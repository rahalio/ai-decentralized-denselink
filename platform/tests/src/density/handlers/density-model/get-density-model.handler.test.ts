/**
 * GetDensityModel handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: density
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getDensityModel } from "@denselink/api-server/domains/density/handlers";
import type { DensityDDDDependencies } from "@denselink/api-server/domains/density";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@denselink/tests/shared";
import { DENSITY_USE_CASES } from "../__factories__/use-cases.js";

describe("GetDensityModel", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DensityDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DensityDDDDependencies>(
      DENSITY_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetDensityModel, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { densityModelId: entity.id };

    await getDensityModel(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetDensityModel.execute).toHaveBeenCalledWith(expect.objectContaining({ densityModelId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { densityModelId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetDensityModel, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getDensityModel(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
