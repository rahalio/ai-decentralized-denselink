/**
 * GetLicenseKey handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: licenses
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getLicenseKey } from "@denselink/api-server/domains/licenses/handlers";
import type { LicensesDDDDependencies } from "@denselink/api-server/domains/licenses";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@denselink/tests/shared";
import { LICENSES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetLicenseKey", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: LicensesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<LicensesDDDDependencies>(
      LICENSES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetLicenseKey, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { licenseId: entity.id };

    await getLicenseKey(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetLicenseKey.execute).toHaveBeenCalledWith(expect.objectContaining({ licenseId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { licenseId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetLicenseKey, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getLicenseKey(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
