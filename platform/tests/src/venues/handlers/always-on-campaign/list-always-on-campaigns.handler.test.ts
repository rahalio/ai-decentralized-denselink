/**
 * ListAlwaysOnCampaigns handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: venues
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listAlwaysOnCampaigns } from "@denselink/api-server/domains/venues/handlers";
import type { VenuesDDDDependencies } from "@denselink/api-server/domains/venues";
import { createAlwaysOnCampaign as createAlwaysOnCampaignEntity } from "@denselink/factories/venues";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@denselink/tests/shared";
import { VENUES_USE_CASES } from "../__factories__/use-cases.js";

describe("ListAlwaysOnCampaigns", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: VenuesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<VenuesDDDDependencies>(
      VENUES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createAlwaysOnCampaignEntity({ orgId }),
      createAlwaysOnCampaignEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListAlwaysOnCampaigns, "execute").mockResolvedValue(useCaseResult);

    await listAlwaysOnCampaigns(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListAlwaysOnCampaigns.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
