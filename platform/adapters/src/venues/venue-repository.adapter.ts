/**
 * VenueRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: venues
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * VenueRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements VenueRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  VenueRepository,
} from "@denselink/services/venues";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { VenueRepositoryDdb } from "./venue-repository.ddb.js";

/**
 * VenueRepositoryAdapter
 *
 * Implements VenueRepository port for infrastructure layer.
 */
export class VenueRepositoryAdapter implements VenueRepository {
  private readonly ddb: VenueRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new VenueRepositoryDdb(this.dynamoClient);
  }

  async listVenueProgrammes(input: Parameters<VenueRepository['listVenueProgrammes']>[0]): Promise<Awaited<ReturnType<VenueRepository['listVenueProgrammes']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listVenueProgrammes(input);
  }

  async createVenueProgramme(input: Parameters<VenueRepository['createVenueProgramme']>[0]): Promise<Awaited<ReturnType<VenueRepository['createVenueProgramme']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.createVenueProgramme(input);
  }

  async getVenueProgramme(input: Parameters<VenueRepository['getVenueProgramme']>[0]): Promise<Awaited<ReturnType<VenueRepository['getVenueProgramme']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getVenueProgramme(input);
  }
}
